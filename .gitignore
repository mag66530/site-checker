# Кеш приложения (sitemap, история ротации) — пересоздаётся сам
cache/

# Сохранённые xlsx-отчёты прогонов
reports/

# Python
__pycache__/
*.pyc
.pytest_cache/

# Streamlit secrets — никогда не коммитить
.streamlit/secrets.toml

# GSC автоматизация — сессия и профиль Chrome
gsc_session.json
gsc_chrome_profile/
gsc_reindex_log.json
gsc_properties.json
urls.txt
gsc_validate_log.json
webmaster_recheck_log.json
admin.local.json
