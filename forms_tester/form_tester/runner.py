"""Оркестрация прогона. Реализация пока в `test_all`; импорт ленивый, чтобы избежать циклов."""


def run_test(ОЧИСТИТЬ_EXCEL=True, stop_flag=None, headless=True,
             город="", почта_получателя="", проба_файлов=False, xss_проба=False,
             валидация_проба=False, лимит_проба=False, проверять_цели=False,
             потоков=None):
    """Обёртка над test_all.run_test.

    ВАЖНО: список аргументов здесь ДУБЛИРУЕТ настоящий - новый параметр движка
    нужно добавлять и сюда, иначе прогон падает на «unexpected keyword
    argument» (так и случилось с «потоков»). Тест
    tests/test_forms_runner_signature.py сверяет обе подписи."""
    from test_all import run_test as _run_test

    return _run_test(ОЧИСТИТЬ_EXCEL=ОЧИСТИТЬ_EXCEL, stop_flag=stop_flag, headless=headless,
                     город=город, почта_получателя=почта_получателя,
                     проба_файлов=проба_файлов, xss_проба=xss_проба,
                     валидация_проба=валидация_проба, лимит_проба=лимит_проба,
                     проверять_цели=проверять_цели, потоков=потоков)
