import streamlit as st

st.title('Проверка форм')
st.caption('Проверка отправки форм — СМУ · ИМП · МПЭ')

st.write(
    'Сервис проверяет, что формы на сайтах принимают заявки: открывает страницу '
    'в браузере, заполняет поля и отправляет, затем фиксирует результат в логе.'
)

project = st.selectbox('Проект', ['СМУ — Сталметурал', 'ИМП — Инметпром', 'МПЭ — Мепэн'])

c1, c2 = st.columns([1, 1])
clear_log = c1.checkbox('Очищать лог перед прогоном', value=True)
run = c2.button('▶ Запустить проверку', type='primary', use_container_width=True)

if run:
    st.warning(
        'Проверка форм открывает реальный браузер (Playwright). Запуск доступен '
        'в десктопной версии или на сервере с браузером — в облачной версии '
        'браузер недоступен.'
    )

st.subheader('Результаты')
st.caption('Лог появится после первого прогона.')
