streamlit>=1.40.0
aiohttp>=3.10.0
openpyxl>=3.1.0

# Для «Проверки форм» и «Проверки целей» в облаке (браузер Playwright).
# Chromium доустанавливается в рантайме (browser_setup.ensure_browser),
# системные библиотеки браузера ставит Streamlit Cloud по packages.txt.
playwright>=1.40.0
requests>=2.28.0
beautifulsoup4>=4.12.0
