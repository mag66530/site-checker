# Локальные зависимости (лаунчер автокликеров, движок «Проверки форм»).
#
# ВНИМАНИЕ: одного этого файла для запуска приложения НЕ хватает - здесь нет
# библиотек личного кабинета (psycopg, bcrypt, extra-streamlit-components,
# cryptography), и START.bat падал бы на `import auth` с ModuleNotFoundError.
# Полный набор - в requirements.txt; «INSTALL (run once).bat» ставит ОБА файла.
# Исторически playwright держали только здесь, чтобы Streamlit Cloud не тянул
# тяжёлый пакет, но с приходом облачных «Проверки форм»/«Проверки целей» он
# всё равно переехал в requirements.txt - и этот файл стал его подмножеством.
#
# Установка (один раз):
#   pip install -r requirements.txt -r requirements-local.txt
#   playwright install chromium
#
# (Chrome для автокликеров используется реальный, но playwright нужен как драйвер.)

streamlit>=1.40.0
playwright>=1.40.0

# Движок «Проверки форм» (forms_tester): отправка форм через requests/Playwright
requests>=2.28.0
beautifulsoup4>=4.12.0
openpyxl>=3.1.0
